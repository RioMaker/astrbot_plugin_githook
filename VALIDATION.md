# 0.1.0 验证记录

日期：2026-09-24。

## 已验证

- Python 3.10.9，`pytest` 45 项通过；Ruff 检查与格式检查通过。
- 权限测试包含普通成员、QQ 管理员、QQ 群主、AstrBot 管理员和私聊；群订阅使用完整 UMO，不同平台同群号互不串用。
- 实际本地 aiohttp HTTP 请求覆盖签名拒绝、ping、分支/仓库过滤、并发相同 Delivery ID、冲突 ID、2 MiB 请求限制、持久队列。
- 本地 GitHub API 测试服务验证跨仓库排序、超过 100 条后的分页、默认 30 条、日期范围北京时间边界、Token/限流错误和旧缓存提示。
- 安装清单测试覆盖静默基线、自有/第三方识别、加载事件唤醒、重载/重装去重、作者范围变更。
- 发送测试覆盖逐群成功/失败、分段顺序、重试耗尽、关闭取消和重启持久化。
- 生命周期测试覆盖成功退出和端口占用初始化失败；临时 HTTP 服务全部关闭，并验证各测试端口不能再连接。
- 单独加载 AstrBot 官方 master 的真实 `CommandFilter`，以最小依赖桩验证空参数、`开` 和含插件/日期/页码的完整参数，共 3 个场景通过。
- 对本工作区的真实插件元数据进行只读扫描，识别出 12 个符合 RioMaker 仓库规则的插件。这个结果不表示这些插件已在某台运行中的 AstrBot 安装。
- 使用插件自己的 GitHub 客户端，无 Token 成功请求 `RioMaker/astrbot_plugin_liuyao`，返回最近 3 条真实提交，无查询警告；最近 SHA 为 `c6a9b67`（仅表示验证当时的结果）。

## 验证边界

- AstrBot 生命周期、权限事件和平台发送采用测试桩；没有启动完整 AstrBot，没有向 QQ 群发送测试消息。
- 未配置用户真实 Secret/Token，未创建远端 Webhook，未部署、发布或推送 Git 仓库。
- 平台声明为 `aiocqhttp`。QQ 官方机器人主动消息接口未实现。
- 上游源码来自验证当时的 master；命令过滤器验证不代表完整框架集成通过。

## 接入时验收

1. 在真实 AstrBot WebUI 配置 owners、Webhook Secret、端口；需要时配置只读 Token。
2. 普通成员及 QQ 群管理员执行 `/githook 开` 应拒绝，AstrBot 管理员应成功。
3. GitHub ping 应成功；push 应返回 queued，并仅向开启群发送一次。
4. 安装一个此前未被 githook 记录的 RioMaker 插件，应收到新增提醒；安装其他作者插件不应提醒。
5. `/githook 历史 六爻 页 2` 和日期范围应返回对应标题与北京时间。
6. 关闭群、重载插件后确认订阅与队列状态符合 README 语义。
